// This file satisfies TypeScript references but isn't needed for Vercel deployment
// It's referenced in the TypeScript configuration but not used in this deployment

export interface Env {
  // Empty interface - not used for Vercel deployment
}
